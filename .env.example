# vigilAI environment — copy to `.env` and fill in. `.env` is gitignored (never commit keys).
#
# Only needed for the hosted "headline" demo backend (Claude Haiku 4.5). The mock and local
# (Ollama) backends need no key. Use a FUNDED console.anthropic.com key, billed separately
# from any Claude subscription.
ANTHROPIC_API_KEY=
